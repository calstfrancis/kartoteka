# Build output
/target

# flatpak-builder working dirs — large, regenerable, never committed
/.flatpak-builder
/build-flatpak

# Vendored native libraries (PDFium prebuilt binary — not redistributed in-repo)
/vendor

# Planning scratch — not part of the shipped repo (see the brief)
Plans/

# Editor swap files
*.kate-swp
*~

# Planning docs (local only, not for the public repo)
EPUB-READER-PLAN.md
docs/M2-GUI-PLAN.md
docs/ROADMAP.md
