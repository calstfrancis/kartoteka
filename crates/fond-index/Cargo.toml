[package]
name = "fond-index"
version = "0.1.0"
description = "Tantivy full-text search over library metadata, notes, annotations, and PDF text — shared Fond crate"
license = "MIT"
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true

[dependencies]
tantivy.workspace = true
fond-bib = { path = "../fond-bib" }
thiserror.workspace = true

[dev-dependencies]
tempfile = "3"
